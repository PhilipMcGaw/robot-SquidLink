# Gazebo model

Begin with a simple underwater ROV consisting of a hull, thrusters, collision geometry, visual geometry, and the required sensor frames. Add realistic hydrodynamics only after the command and telemetry loop is repeatable.

The model should support controlled initial conditions and deterministic scenario execution. Changes to mass, thrust, drag, buoyancy, or coordinate frames must be documented because they alter test results.
